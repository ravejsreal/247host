{
  "version": 2,
  "builds": [
    {
      "src": "api/app.py",  // Ensure this matches your file structure
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/app.py"
    }
  ]
}
